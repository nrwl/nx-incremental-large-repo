import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib0childlib4component46Component } from './lib0childlib4component46.component';

describe('Lib0childlib4component46Component', () => {
  let component: Lib0childlib4component46Component;
  let fixture: ComponentFixture<Lib0childlib4component46Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [Lib0childlib4component46Component]
    }).compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib0childlib4component46Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
