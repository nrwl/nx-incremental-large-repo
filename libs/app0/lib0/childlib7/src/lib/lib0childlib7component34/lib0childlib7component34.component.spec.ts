import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib0childlib7component34Component } from './lib0childlib7component34.component';

describe('Lib0childlib7component34Component', () => {
  let component: Lib0childlib7component34Component;
  let fixture: ComponentFixture<Lib0childlib7component34Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [Lib0childlib7component34Component]
    }).compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib0childlib7component34Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
