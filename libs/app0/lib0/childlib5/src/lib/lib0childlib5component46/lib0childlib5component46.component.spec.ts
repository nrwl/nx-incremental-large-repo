import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib0childlib5component46Component } from './lib0childlib5component46.component';

describe('Lib0childlib5component46Component', () => {
  let component: Lib0childlib5component46Component;
  let fixture: ComponentFixture<Lib0childlib5component46Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [Lib0childlib5component46Component]
    }).compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib0childlib5component46Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
