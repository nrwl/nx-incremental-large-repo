import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib0component40 from './lib0childlib0component40';

describe('Lib0childlib0component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib0component40 />);
    expect(baseElement).toBeTruthy();
  });
});
