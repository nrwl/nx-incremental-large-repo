import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component22 from './lib0childlib5component22';

describe('Lib0childlib5component22', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component22 />);
    expect(baseElement).toBeTruthy();
  });
});
