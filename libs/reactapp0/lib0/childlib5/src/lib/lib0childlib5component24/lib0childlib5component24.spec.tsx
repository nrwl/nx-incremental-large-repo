import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component24 from './lib0childlib5component24';

describe('Lib0childlib5component24', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component24 />);
    expect(baseElement).toBeTruthy();
  });
});
