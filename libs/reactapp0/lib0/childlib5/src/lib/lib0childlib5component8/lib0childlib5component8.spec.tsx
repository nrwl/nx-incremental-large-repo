import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component8 from './lib0childlib5component8';

describe('Lib0childlib5component8', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component8 />);
    expect(baseElement).toBeTruthy();
  });
});
