import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component15 from './lib0childlib5component15';

describe('Lib0childlib5component15', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component15 />);
    expect(baseElement).toBeTruthy();
  });
});
