import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component40 from './lib0childlib5component40';

describe('Lib0childlib5component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component40 />);
    expect(baseElement).toBeTruthy();
  });
});
