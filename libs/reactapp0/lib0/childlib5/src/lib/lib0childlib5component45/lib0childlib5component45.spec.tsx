import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component45 from './lib0childlib5component45';

describe('Lib0childlib5component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component45 />);
    expect(baseElement).toBeTruthy();
  });
});
