import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component38 from './lib0childlib5component38';

describe('Lib0childlib5component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component38 />);
    expect(baseElement).toBeTruthy();
  });
});
