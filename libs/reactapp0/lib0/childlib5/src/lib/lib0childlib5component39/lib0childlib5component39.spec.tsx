import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component39 from './lib0childlib5component39';

describe('Lib0childlib5component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component39 />);
    expect(baseElement).toBeTruthy();
  });
});
