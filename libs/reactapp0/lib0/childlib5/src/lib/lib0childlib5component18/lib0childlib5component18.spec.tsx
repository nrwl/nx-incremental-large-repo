import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib5component18 from './lib0childlib5component18';

describe('Lib0childlib5component18', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib5component18 />);
    expect(baseElement).toBeTruthy();
  });
});
