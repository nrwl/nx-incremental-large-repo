import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib1component48 from './lib0childlib1component48';

describe('Lib0childlib1component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib1component48 />);
    expect(baseElement).toBeTruthy();
  });
});
