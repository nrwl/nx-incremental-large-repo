import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib1component44 from './lib0childlib1component44';

describe('Lib0childlib1component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib1component44 />);
    expect(baseElement).toBeTruthy();
  });
});
