import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib1component40 from './lib0childlib1component40';

describe('Lib0childlib1component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib1component40 />);
    expect(baseElement).toBeTruthy();
  });
});
