import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib1component38 from './lib0childlib1component38';

describe('Lib0childlib1component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib1component38 />);
    expect(baseElement).toBeTruthy();
  });
});
