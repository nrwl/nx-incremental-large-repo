import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib1component25 from './lib0childlib1component25';

describe('Lib0childlib1component25', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib1component25 />);
    expect(baseElement).toBeTruthy();
  });
});
