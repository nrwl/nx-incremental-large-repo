import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component8 from './lib0childlib7component8';

describe('Lib0childlib7component8', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component8 />);
    expect(baseElement).toBeTruthy();
  });
});
