import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component31 from './lib0childlib7component31';

describe('Lib0childlib7component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component31 />);
    expect(baseElement).toBeTruthy();
  });
});
