import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component24 from './lib0childlib7component24';

describe('Lib0childlib7component24', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component24 />);
    expect(baseElement).toBeTruthy();
  });
});
