import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component14 from './lib0childlib7component14';

describe('Lib0childlib7component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component14 />);
    expect(baseElement).toBeTruthy();
  });
});
