import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component25 from './lib0childlib7component25';

describe('Lib0childlib7component25', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component25 />);
    expect(baseElement).toBeTruthy();
  });
});
