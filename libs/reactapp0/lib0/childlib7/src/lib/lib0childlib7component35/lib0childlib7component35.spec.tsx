import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component35 from './lib0childlib7component35';

describe('Lib0childlib7component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component35 />);
    expect(baseElement).toBeTruthy();
  });
});
