import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component40 from './lib0childlib7component40';

describe('Lib0childlib7component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component40 />);
    expect(baseElement).toBeTruthy();
  });
});
