import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component15 from './lib0childlib7component15';

describe('Lib0childlib7component15', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component15 />);
    expect(baseElement).toBeTruthy();
  });
});
