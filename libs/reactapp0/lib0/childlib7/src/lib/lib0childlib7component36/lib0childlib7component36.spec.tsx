import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib7component36 from './lib0childlib7component36';

describe('Lib0childlib7component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib7component36 />);
    expect(baseElement).toBeTruthy();
  });
});
