import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib6component16 from './lib0childlib6component16';

describe('Lib0childlib6component16', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib6component16 />);
    expect(baseElement).toBeTruthy();
  });
});
