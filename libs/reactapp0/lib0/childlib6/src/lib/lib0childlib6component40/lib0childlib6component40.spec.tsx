import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib6component40 from './lib0childlib6component40';

describe('Lib0childlib6component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib6component40 />);
    expect(baseElement).toBeTruthy();
  });
});
