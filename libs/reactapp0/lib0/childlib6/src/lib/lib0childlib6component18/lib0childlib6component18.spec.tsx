import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib6component18 from './lib0childlib6component18';

describe('Lib0childlib6component18', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib6component18 />);
    expect(baseElement).toBeTruthy();
  });
});
