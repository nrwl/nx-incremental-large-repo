import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib2component25 from './lib0childlib2component25';

describe('Lib0childlib2component25', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib2component25 />);
    expect(baseElement).toBeTruthy();
  });
});
