import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib2component48 from './lib0childlib2component48';

describe('Lib0childlib2component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib2component48 />);
    expect(baseElement).toBeTruthy();
  });
});
