import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib2component44 from './lib0childlib2component44';

describe('Lib0childlib2component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib2component44 />);
    expect(baseElement).toBeTruthy();
  });
});
