import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib2component29 from './lib0childlib2component29';

describe('Lib0childlib2component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib2component29 />);
    expect(baseElement).toBeTruthy();
  });
});
