import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib8component40 from './lib0childlib8component40';

describe('Lib0childlib8component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib8component40 />);
    expect(baseElement).toBeTruthy();
  });
});
