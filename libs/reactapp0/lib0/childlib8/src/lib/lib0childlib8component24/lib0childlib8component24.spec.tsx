import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib8component24 from './lib0childlib8component24';

describe('Lib0childlib8component24', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib8component24 />);
    expect(baseElement).toBeTruthy();
  });
});
