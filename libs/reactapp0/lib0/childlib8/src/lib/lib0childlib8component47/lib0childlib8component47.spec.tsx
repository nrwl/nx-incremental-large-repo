import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib8component47 from './lib0childlib8component47';

describe('Lib0childlib8component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib8component47 />);
    expect(baseElement).toBeTruthy();
  });
});
