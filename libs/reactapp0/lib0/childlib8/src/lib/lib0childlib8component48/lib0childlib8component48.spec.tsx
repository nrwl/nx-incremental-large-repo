import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib8component48 from './lib0childlib8component48';

describe('Lib0childlib8component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib8component48 />);
    expect(baseElement).toBeTruthy();
  });
});
