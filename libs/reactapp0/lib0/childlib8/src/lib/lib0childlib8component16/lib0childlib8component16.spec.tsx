import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib8component16 from './lib0childlib8component16';

describe('Lib0childlib8component16', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib8component16 />);
    expect(baseElement).toBeTruthy();
  });
});
