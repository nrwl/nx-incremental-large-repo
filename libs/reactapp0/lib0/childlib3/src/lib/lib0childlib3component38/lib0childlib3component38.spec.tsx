import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib3component38 from './lib0childlib3component38';

describe('Lib0childlib3component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib3component38 />);
    expect(baseElement).toBeTruthy();
  });
});
