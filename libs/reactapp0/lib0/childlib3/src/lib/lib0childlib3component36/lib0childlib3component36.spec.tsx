import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib3component36 from './lib0childlib3component36';

describe('Lib0childlib3component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib3component36 />);
    expect(baseElement).toBeTruthy();
  });
});
