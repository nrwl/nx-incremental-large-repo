import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib3component35 from './lib0childlib3component35';

describe('Lib0childlib3component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib3component35 />);
    expect(baseElement).toBeTruthy();
  });
});
