import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib3component44 from './lib0childlib3component44';

describe('Lib0childlib3component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib3component44 />);
    expect(baseElement).toBeTruthy();
  });
});
