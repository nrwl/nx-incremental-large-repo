import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib3component30 from './lib0childlib3component30';

describe('Lib0childlib3component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib3component30 />);
    expect(baseElement).toBeTruthy();
  });
});
