import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib4component18 from './lib0childlib4component18';

describe('Lib0childlib4component18', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib4component18 />);
    expect(baseElement).toBeTruthy();
  });
});
