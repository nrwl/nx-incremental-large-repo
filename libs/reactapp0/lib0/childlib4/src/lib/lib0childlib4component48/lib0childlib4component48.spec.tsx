import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib4component48 from './lib0childlib4component48';

describe('Lib0childlib4component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib4component48 />);
    expect(baseElement).toBeTruthy();
  });
});
