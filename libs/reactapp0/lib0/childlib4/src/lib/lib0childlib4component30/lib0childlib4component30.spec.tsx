import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib4component30 from './lib0childlib4component30';

describe('Lib0childlib4component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib4component30 />);
    expect(baseElement).toBeTruthy();
  });
});
