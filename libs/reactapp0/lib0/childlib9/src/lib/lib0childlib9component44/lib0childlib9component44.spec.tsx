import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib9component44 from './lib0childlib9component44';

describe('Lib0childlib9component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib9component44 />);
    expect(baseElement).toBeTruthy();
  });
});
