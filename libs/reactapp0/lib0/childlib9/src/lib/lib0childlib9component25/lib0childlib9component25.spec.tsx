import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib9component25 from './lib0childlib9component25';

describe('Lib0childlib9component25', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib9component25 />);
    expect(baseElement).toBeTruthy();
  });
});
