import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib9component26 from './lib0childlib9component26';

describe('Lib0childlib9component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib9component26 />);
    expect(baseElement).toBeTruthy();
  });
});
