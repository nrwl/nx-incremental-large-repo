import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib9component8 from './lib0childlib9component8';

describe('Lib0childlib9component8', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib9component8 />);
    expect(baseElement).toBeTruthy();
  });
});
