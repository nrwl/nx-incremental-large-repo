import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib9component20 from './lib0childlib9component20';

describe('Lib0childlib9component20', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib9component20 />);
    expect(baseElement).toBeTruthy();
  });
});
