import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib9component22 from './lib0childlib9component22';

describe('Lib0childlib9component22', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib9component22 />);
    expect(baseElement).toBeTruthy();
  });
});
