import React from 'react';
import { render } from '@testing-library/react';

import Lib0childlib9component35 from './lib0childlib9component35';

describe('Lib0childlib9component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib0childlib9component35 />);
    expect(baseElement).toBeTruthy();
  });
});
