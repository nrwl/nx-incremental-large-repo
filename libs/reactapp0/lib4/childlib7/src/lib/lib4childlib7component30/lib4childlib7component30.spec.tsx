import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib7component30 from './lib4childlib7component30';

describe('Lib4childlib7component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib7component30 />);
    expect(baseElement).toBeTruthy();
  });
});
