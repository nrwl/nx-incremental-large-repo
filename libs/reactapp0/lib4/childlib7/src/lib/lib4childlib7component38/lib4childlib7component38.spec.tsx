import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib7component38 from './lib4childlib7component38';

describe('Lib4childlib7component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib7component38 />);
    expect(baseElement).toBeTruthy();
  });
});
