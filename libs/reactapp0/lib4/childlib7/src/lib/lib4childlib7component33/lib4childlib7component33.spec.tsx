import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib7component33 from './lib4childlib7component33';

describe('Lib4childlib7component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib7component33 />);
    expect(baseElement).toBeTruthy();
  });
});
