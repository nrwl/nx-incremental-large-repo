import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib2component47 from './lib4childlib2component47';

describe('Lib4childlib2component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib2component47 />);
    expect(baseElement).toBeTruthy();
  });
});
