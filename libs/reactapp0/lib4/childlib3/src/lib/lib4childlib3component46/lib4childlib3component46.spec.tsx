import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib3component46 from './lib4childlib3component46';

describe('Lib4childlib3component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib3component46 />);
    expect(baseElement).toBeTruthy();
  });
});
