import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib9component40 from './lib4childlib9component40';

describe('Lib4childlib9component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib9component40 />);
    expect(baseElement).toBeTruthy();
  });
});
