import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib4component28 from './lib4childlib4component28';

describe('Lib4childlib4component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib4component28 />);
    expect(baseElement).toBeTruthy();
  });
});
