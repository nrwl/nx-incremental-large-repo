import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib4component38 from './lib4childlib4component38';

describe('Lib4childlib4component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib4component38 />);
    expect(baseElement).toBeTruthy();
  });
});
