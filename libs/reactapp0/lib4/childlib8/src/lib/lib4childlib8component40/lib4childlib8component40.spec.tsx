import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib8component40 from './lib4childlib8component40';

describe('Lib4childlib8component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib8component40 />);
    expect(baseElement).toBeTruthy();
  });
});
