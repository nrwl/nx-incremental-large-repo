import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib8component49 from './lib4childlib8component49';

describe('Lib4childlib8component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib8component49 />);
    expect(baseElement).toBeTruthy();
  });
});
