import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib5component29 from './lib4childlib5component29';

describe('Lib4childlib5component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib5component29 />);
    expect(baseElement).toBeTruthy();
  });
});
