import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib5component39 from './lib4childlib5component39';

describe('Lib4childlib5component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib5component39 />);
    expect(baseElement).toBeTruthy();
  });
});
