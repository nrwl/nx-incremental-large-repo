import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib0component38 from './lib4childlib0component38';

describe('Lib4childlib0component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib0component38 />);
    expect(baseElement).toBeTruthy();
  });
});
