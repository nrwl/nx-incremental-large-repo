import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib0component48 from './lib4childlib0component48';

describe('Lib4childlib0component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib0component48 />);
    expect(baseElement).toBeTruthy();
  });
});
