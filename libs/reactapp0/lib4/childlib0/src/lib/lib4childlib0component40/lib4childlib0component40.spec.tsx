import React from 'react';
import { render } from '@testing-library/react';

import Lib4childlib0component40 from './lib4childlib0component40';

describe('Lib4childlib0component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib4childlib0component40 />);
    expect(baseElement).toBeTruthy();
  });
});
