import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib9component44 from './lib1childlib9component44';

describe('Lib1childlib9component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib9component44 />);
    expect(baseElement).toBeTruthy();
  });
});
