import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib1component38 from './lib1childlib1component38';

describe('Lib1childlib1component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib1component38 />);
    expect(baseElement).toBeTruthy();
  });
});
