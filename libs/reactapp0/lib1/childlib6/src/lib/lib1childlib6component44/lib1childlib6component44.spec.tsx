import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib6component44 from './lib1childlib6component44';

describe('Lib1childlib6component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib6component44 />);
    expect(baseElement).toBeTruthy();
  });
});
