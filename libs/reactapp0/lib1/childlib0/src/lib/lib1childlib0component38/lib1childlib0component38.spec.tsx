import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib0component38 from './lib1childlib0component38';

describe('Lib1childlib0component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib0component38 />);
    expect(baseElement).toBeTruthy();
  });
});
