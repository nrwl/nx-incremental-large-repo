import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib0component40 from './lib1childlib0component40';

describe('Lib1childlib0component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib0component40 />);
    expect(baseElement).toBeTruthy();
  });
});
