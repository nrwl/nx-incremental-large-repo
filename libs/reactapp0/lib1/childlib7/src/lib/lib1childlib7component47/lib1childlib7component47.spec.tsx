import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib7component47 from './lib1childlib7component47';

describe('Lib1childlib7component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib7component47 />);
    expect(baseElement).toBeTruthy();
  });
});
