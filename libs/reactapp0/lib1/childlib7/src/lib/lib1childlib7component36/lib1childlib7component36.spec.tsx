import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib7component36 from './lib1childlib7component36';

describe('Lib1childlib7component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib7component36 />);
    expect(baseElement).toBeTruthy();
  });
});
