import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib7component33 from './lib1childlib7component33';

describe('Lib1childlib7component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib7component33 />);
    expect(baseElement).toBeTruthy();
  });
});
