import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib7component48 from './lib1childlib7component48';

describe('Lib1childlib7component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib7component48 />);
    expect(baseElement).toBeTruthy();
  });
});
