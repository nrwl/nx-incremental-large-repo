import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib5component39 from './lib1childlib5component39';

describe('Lib1childlib5component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib5component39 />);
    expect(baseElement).toBeTruthy();
  });
});
