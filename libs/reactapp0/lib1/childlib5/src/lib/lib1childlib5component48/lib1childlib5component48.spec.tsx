import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib5component48 from './lib1childlib5component48';

describe('Lib1childlib5component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib5component48 />);
    expect(baseElement).toBeTruthy();
  });
});
