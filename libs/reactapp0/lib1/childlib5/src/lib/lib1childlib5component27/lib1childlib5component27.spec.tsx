import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib5component27 from './lib1childlib5component27';

describe('Lib1childlib5component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib5component27 />);
    expect(baseElement).toBeTruthy();
  });
});
