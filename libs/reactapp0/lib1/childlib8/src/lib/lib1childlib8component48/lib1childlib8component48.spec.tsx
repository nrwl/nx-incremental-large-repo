import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib8component48 from './lib1childlib8component48';

describe('Lib1childlib8component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib8component48 />);
    expect(baseElement).toBeTruthy();
  });
});
