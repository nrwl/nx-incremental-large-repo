import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib8component38 from './lib1childlib8component38';

describe('Lib1childlib8component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib8component38 />);
    expect(baseElement).toBeTruthy();
  });
});
