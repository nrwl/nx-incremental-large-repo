import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib8component19 from './lib1childlib8component19';

describe('Lib1childlib8component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib8component19 />);
    expect(baseElement).toBeTruthy();
  });
});
