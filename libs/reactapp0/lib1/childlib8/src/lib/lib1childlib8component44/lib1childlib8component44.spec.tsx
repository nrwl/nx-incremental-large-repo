import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib8component44 from './lib1childlib8component44';

describe('Lib1childlib8component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib8component44 />);
    expect(baseElement).toBeTruthy();
  });
});
