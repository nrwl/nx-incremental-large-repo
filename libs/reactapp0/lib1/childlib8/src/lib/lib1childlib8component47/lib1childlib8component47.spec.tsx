import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib8component47 from './lib1childlib8component47';

describe('Lib1childlib8component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib8component47 />);
    expect(baseElement).toBeTruthy();
  });
});
