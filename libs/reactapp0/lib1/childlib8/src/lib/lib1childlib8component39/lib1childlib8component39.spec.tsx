import React from 'react';
import { render } from '@testing-library/react';

import Lib1childlib8component39 from './lib1childlib8component39';

describe('Lib1childlib8component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib1childlib8component39 />);
    expect(baseElement).toBeTruthy();
  });
});
