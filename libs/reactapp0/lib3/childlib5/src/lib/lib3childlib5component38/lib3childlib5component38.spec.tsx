import React from 'react';
import { render } from '@testing-library/react';

import Lib3childlib5component38 from './lib3childlib5component38';

describe('Lib3childlib5component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib3childlib5component38 />);
    expect(baseElement).toBeTruthy();
  });
});
