import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib5component48 from './lib2childlib5component48';

describe('Lib2childlib5component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib5component48 />);
    expect(baseElement).toBeTruthy();
  });
});
