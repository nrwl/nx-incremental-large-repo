import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib5component44 from './lib2childlib5component44';

describe('Lib2childlib5component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib5component44 />);
    expect(baseElement).toBeTruthy();
  });
});
