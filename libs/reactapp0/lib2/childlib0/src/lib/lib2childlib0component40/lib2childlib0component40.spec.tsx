import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib0component40 from './lib2childlib0component40';

describe('Lib2childlib0component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib0component40 />);
    expect(baseElement).toBeTruthy();
  });
});
