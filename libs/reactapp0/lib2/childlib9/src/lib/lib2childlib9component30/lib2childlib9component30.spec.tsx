import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib9component30 from './lib2childlib9component30';

describe('Lib2childlib9component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib9component30 />);
    expect(baseElement).toBeTruthy();
  });
});
