import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib9component44 from './lib2childlib9component44';

describe('Lib2childlib9component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib9component44 />);
    expect(baseElement).toBeTruthy();
  });
});
