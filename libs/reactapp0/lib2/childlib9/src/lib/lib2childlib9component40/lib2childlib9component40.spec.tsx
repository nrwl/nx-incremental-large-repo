import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib9component40 from './lib2childlib9component40';

describe('Lib2childlib9component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib9component40 />);
    expect(baseElement).toBeTruthy();
  });
});
