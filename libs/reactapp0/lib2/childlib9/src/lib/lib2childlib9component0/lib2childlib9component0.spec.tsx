import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib9component0 from './lib2childlib9component0';

describe('Lib2childlib9component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib9component0 />);
    expect(baseElement).toBeTruthy();
  });
});
