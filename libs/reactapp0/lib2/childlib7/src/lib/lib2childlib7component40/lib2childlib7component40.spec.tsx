import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib7component40 from './lib2childlib7component40';

describe('Lib2childlib7component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib7component40 />);
    expect(baseElement).toBeTruthy();
  });
});
