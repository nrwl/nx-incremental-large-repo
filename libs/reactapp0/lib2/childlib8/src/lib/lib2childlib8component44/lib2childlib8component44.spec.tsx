import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib8component44 from './lib2childlib8component44';

describe('Lib2childlib8component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib8component44 />);
    expect(baseElement).toBeTruthy();
  });
});
