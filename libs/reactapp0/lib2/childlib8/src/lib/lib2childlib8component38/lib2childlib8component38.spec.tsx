import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib8component38 from './lib2childlib8component38';

describe('Lib2childlib8component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib8component38 />);
    expect(baseElement).toBeTruthy();
  });
});
