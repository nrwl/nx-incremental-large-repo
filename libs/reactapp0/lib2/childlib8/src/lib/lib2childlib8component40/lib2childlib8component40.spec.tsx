import React from 'react';
import { render } from '@testing-library/react';

import Lib2childlib8component40 from './lib2childlib8component40';

describe('Lib2childlib8component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Lib2childlib8component40 />);
    expect(baseElement).toBeTruthy();
  });
});
